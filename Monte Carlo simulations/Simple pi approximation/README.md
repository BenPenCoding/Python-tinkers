# Simple pi approximation Monte Carlo simulation

#### By simulating a pin drop on a board that contains a square and a circle, the square with side length r and circle with radius r, I manipulate the number of times the pin drops on either shape to produce an approximation of pi.
